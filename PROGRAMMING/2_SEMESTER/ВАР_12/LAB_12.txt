#include "stdafx.h"
#include <iostream>
#include <math.h>
#include <locale.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>
#include <windows.h>

using namespace std;

int main()
{
	const int n = 100, kol = 100;
	typedef char String[n];
	
	cout << "Input string of nambers" << endl;
	String str;
	cin.getline(str, kol);

	cout << "Convert string to numbers" << endl;
	int numbers[100];
	int count_numbers = 0;
	int j = 0;

	for (int i = 0; i < strlen(str); i+=5)
	{
		String tmp;
		tmp[0] = str[i];
		tmp[1] = str[i+1];
		tmp[2] = str[i+2];
		tmp[3] = str[i+3];
		tmp[4] = str[i+4];
		
		int len = strlen(tmp);
		if (len < 5)
		{
			strncat_s(tmp, str, 5 - len);
		}

		numbers[j] = atoi(tmp);
		j++;
		count_numbers++;
	}

	for (int i = 0; i < count_numbers; i++)
	{
		cout << i + 1 << '.' << numbers[i] << endl;
	}

	system("pause");
	return 0;
}